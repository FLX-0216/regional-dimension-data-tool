# 2026-08-28 工作日志

## 第二十三轮（Streamlit 界面重构：左栏操作 / 右栏结果）
- 需求：
  1. 所有上传及处理数据的操作放到最左侧（sidebar）。
  2. 产线维度 Mapping 上传不再单独成模块，合并进「1. 数据上传与处理」；下次上传覆盖替换之前的 Mapping 明细。
  3. 数据池状态改为可收起（st.expander，默认 collapsed）；需要清理时点击展开。
  4. 「导出明细」改名为「导出设置」；左侧选完筛选/透视选项后，右侧主区域展示明细预览+图表+导出按钮。
- 实现（`app.py` main 整体重写）：
  - `with st.sidebar:` 内依次放：1. 数据上传与处理（含 Mapping 上传合并）、2. 数据池状态/清理（expander，expanded=有 clear_result 时自动展开）、3. 导出设置（筛选+透视选项+【运行筛选/透视】按钮）。
  - 右侧主区域：`st.title` + 引言；`if not run: st.info` 占位；`run` 后执行加载→应用 Mapping→过滤→展示（成功行数/预览/图表/下载按钮）。
  - Mapping 上传按钮改名「保存 Mapping（覆盖）」，`save_mapping` 全量覆写（原本就是覆写，未改动逻辑，仅合并 UI + 文案明确）。
  - 清空按钮的 `clear_result` 成功提示移入 expander 内，且清理后 expander 自动展开以可见提示。
- 编译通过；全杀 python 进程后单实例干净重启（HTTP 200，启动日志仅 Uvicorn started，无报错）。

## 第二十四轮（说明文字字体缩小）
- 需求：把各段说明文字字体缩小。
- 实现（`app.py`）：
  - 将 `st.markdown` 形式的说明文字统一用 `<small>...</small>` 包起来，并加 `unsafe_allow_html=True`。
  - 涉及位置：
    - 左侧「1. 数据上传与处理」引导说明。
    - 左侧「产线维度 Mapping 上传」引导说明。
    - 左侧「3. 导出设置」引导说明。
    - 右侧主区域顶部引言说明。
    - 图表区「自定义透视」下方提示说明。
- 编译通过；干净重启（HTTP 200），无报错。

## 第二十五轮（右侧新增 FCST 分析模块）
- 需求：右侧做 FCST 分析。顶部筛选（财年财季 / 当前 FCST Cycle / 对比 FCST Cycle / 范围 TTL+8大区 / 战区）；表头=当前Cycle金额(整数)、DG%、Quota%、YOY%、WTW(当前-对比)；行=TTL合计→分层APOS/POS→各下层产线大类(Mapping)；WTW 下钻（TTL时每行WTW可点→各大区WTW→点大区→变动最大前5客户名称）。
- 口径确认（AskUserQuestion）：DG%/Quota% 最细维度对齐；YOY 取历史Union去年同期；最终用户=客户名称。
- 实现（`app.py`）：
  - 右侧 `st.tabs(["FCST 分析","明细/透视导出"])`；FCST 分析独立函数 `render_fcst_analysis` + `compute_fcst` + `_build_section`。
  - 数据：FCST 桶(按 财年财季+Cycle 过滤) 用 apply_mapping 生成 产线大类；DG&Quota 桶(数据类别 DG/Quota) 作 DG%/Quota% 分子；历史Union 桶(去年同期 FY) 作 YOY 分母。
  - **关键 bug 修复**：DG/Quota/YOY 目标值原先按明细行 merge 后被行数倍乘放大（DG% 曾达 28363%）。改为对 FCST 明细按 keys 去重后再 sum 目标值。
  - 对齐维度：DG/Quota 用 `_FCST_KEYS=[大区,战区,POS_APOS,物料通路]`（最细）；YOY 用 `_YOY_KEYS=[大区,战区,POS_APOS]`（历史物料通路维度与 FCST 不一致，放宽避免同比失真）。三者均保持同口径（FCST 已覆盖业务 vs 去年同期同业务），因此 TTL YOY≈+13.7% 与 DG%≈94.2% 自洽。
  - 下钻：`st.session_state["fcst_drill"]={level,region}`；level0 TTL 表 + "下钻按各大区"按钮→level1 各大区 WTW 列表(▾按钮)→level2 该大区 top5 客户(按|WTW|降序)，含返回按钮。
  - 范围选大区时额外出战区 selectbox 过滤（不下钻，直接过滤）。
- 离线验证：TTL DG%=94.2%、Quota%=91.5%（FCST 累计 473K vs DG 445K/Quota 433K）；各大区 WTW、华东 top5 客户 WTW 均正常。
- 编译通过；全杀 python 后单实例干净重启（HTTP 200），无报错。
- 待用户确认：YOY 同口径口径（FCST 仅覆盖历史约19%业务组合，故 YOY 反映已覆盖部分同比；若需全量同比需另定分母）。

## 第二十六轮（FCST 分析 UI 与口径修正）
- 需求（6 点）：
  1. 财年财季、范围选择改成横向圆角矩形按钮组（不要 radio 圆圈）。
  2. 战区也横向按钮组展示，点选切换。
  3. 不要“运行 FCST 分析”按钮，点完上方按钮下面实时跟着变。
  4. DG%/Quota% 修正：只有 TTL/APOS/POS 汇总行计算（当前 FCST 金额 / 当前 FY 的 DG&Quota 对应层级）；by 产线大类行不计算 DG%/Quota%。YOY 的 TTL/APOS/POS 保持正确；by 产线大类的 YOY 改为按 大区+战区+POS_APOS+产线大类 对齐（历史Union 也应用 Mapping）。
  5. 去掉“名称”列中的产线大类，产线大类直接作为 APOS/POS 下层行展示在“层级”列。
  6. WTW 下钻不要先点“下钻”按钮：直接在 APOS/POS 行下层用 expander 展示各大区 WTW，各大区再展开看 Top5 客户。
- 实现（`app.py`）：
  - 新增 `_segmented_buttons`：用 `st.columns + st.button(primary/secondary)` 模拟横向圆角按钮组；每次点击 `st.rerun()`。
  - 重写 `render_fcst_analysis`：
    - 财年财季、范围、战区均走 `_segmented_buttons`；当前/对比 Cycle 用 `st.selectbox` 并排。
    - 移除“运行 FCST 分析”按钮；选择即调用 `compute_fcst` 并展示结果。
    - 看板表下方直接放 APOS/POS 两个 `st.expander`，标题含 WTW，展开即下钻各大区；各大区 expander 内再下钻 Top5 客户。
  - 重写 `compute_fcst`：
    - DG&Quota、历史Union 均 `apply_mapping`，确保历史数据有 `产线大类`。
    - 新增 `h_map_pl`：历史Union 按 `_YOY_KEYS + [产线大类]` 聚合，供产线大类行 YOY。
  - 重写 `_build_section`：
    - `add()` 增加 `calc_ratio`（是否计算 DG%/Quota%）和 `calc_yoy_by_pl`（是否按产线大类算 YOY）。
    - TTL/APOS/POS 行 `calc_ratio=True, calc_yoy_by_pl=False`；产线大类行 `calc_ratio=False, calc_yoy_by_pl=True`。
    - 产线大类行“层级”列直接写缩进的产线大类名，“名称”列留空；返回表仍保留“名称”列供 UI 层 drop。
- 离线验证：
  - TTL：DG%=94.2%、Quota%=91.5%、YOY=13.7%；APOS：DG%=97.9%、Quota%=90.7%、YOY=29.6%；POS：DG%=91.9%、Quota%=91.9%、YOY=5.8%。
  - 产线大类行 DG%/Quota% 为空，YOY 有值。
  - 华东大区下 APOS/POS 分层及产线大类均正常。
- 编译通过；旧进程杀掉、Streamlit 干净重启（HTTP 200），无报错。

## 第二十七轮（FCST 分析：按产线大类 WTW 下钻）
- 需求：点击 APOS/POS 下某个产线大类的 WTW 数字（如“联想擎天” -1926），下钻查看：
  1. 该产线大类在各大区的 WTW 增减变动；
  2. 再点击某大区，显示该产线大类 + 大区下变动最大的前 5 名最终用户（客户名称）。
- 实现（`app.py`）：
  - 新增 `_render_pl_wtw(cur_full, cmp_full, pos_apos)`：按产线大类聚合 WTW，每个产线大类一个 `st.expander`；展开后按大区聚合 WTW，每个大区一个子 expander；再展开显示 Top5 客户（按 |WTW| 降序）。
  - 在 APOS/POS 的 WTW 下钻折叠面板内，增加两个子折叠：「按大区下钻」（原逻辑）和「按产线大类下钻」（新逻辑）。
- 验证：编译通过；旧进程杀掉、Streamlit 干净重启（HTTP 200），无报错。

## 第二十八轮（FCST 分析：树表内置下钻 + 性能缓存 + 与导出设置隔离）
- 需求（3 点）：
  1. 左侧导出设置不影响右侧 FCST 分析；FCST 分析基于右侧自身选择的财年财季/Cycle 独立计算。
  2. 提升 FCST 分析加载速度。
  3. 去掉底部下钻面板；主表中 APOS/POS 下每个产线大类下层直接加各大区，大区下层直接加变动最大前 5 客户；新增「上版FCST」列；产线大类/大区/客户行只展示 当前FCST、上版FCST、WTW。
- 实现（`app.py`）：
  - **与导出设置隔离**：`render_fcst_analysis` 用 `fcst_cache_key = fy|cur_cycle|cmp_cycle|scope|sub_region|mapping_mtime` 缓存结果；只有 FCST 自身筛选条件变化时才重新 `compute_fcst`，左侧导出设置变化时直接复用缓存。
  - **性能优化**：新增 `@st.cache_data _load_fcst_analysis_data(fy, mapping_mtime)`，把最耗时的「读 Parquet + apply_mapping + 聚合 DG&Quota/历史Union」按财年财季缓存；切换 Cycle/范围/战区时只切片并构建展示表。
  - **树表下钻**：重写 `_build_section` 为五级层级：`TTL` → `APOS/POS` → `产线大类` → `大区` → `客户`（Top5 by |WTW|）；主表列改为「层级、当前FCST、上版FCST、WTW、DG%、Quota%、YOY%」。
  - **口径**：DG%/Quota%/YOY% 仅 TTL/APOS/POS 汇总行计算；产线大类/大区/客户行 DG%/Quota%/YOY% 留空。
  - 移除底部 WTW 下钻 expander 及 `_render_region_wtw`、`_render_pl_wtw`。
- 离线验证：FY26Q2 Week9 vs Week8 TTL 树表共 563 行；TTL DG%=94.2%、Quota%=91.5%、YOY=13.7%；APOS/POS 分层及产线大类→大区→Top5 客户均正常。
- 编译通过；旧进程杀掉、Streamlit 干净重启（HTTP 200），启动日志无报错。

## 第三十轮（FCST 分析：树表默认折叠 + 修复 Top5 客户 WTW 逻辑）
- 需求（2 点）：
  1. 产线大类下的大区、以及大区下的 WTW 变动最大前 5 名客户，默认折叠，仅点击展开角标后显示。
  2. 修复 Top5 客户 WTW 逻辑：以截图「ESG 服务 / 东南大区」为例，大区 WTW = -142，但只列出 -64 的客户，说明遗漏了当前版无记录但对比版有记录的客户。
- 实现（`app.py`）：
  - **树表折叠展示**：重写 `_build_section` 为每行生成 `id / parent_id / level / label` 结构；新增 `_build_tree_html` + `_render_tree_table`，用 `st.components.v1.html` 渲染可折叠 HTML 树表。
    - 默认显示：TTL、APOS/POS、产线大类（level 0-2）。
    - 默认折叠：大区（level 3）、客户（level 4）。
    - 可展开行前面显示 ▶，点击后变为 ▼ 并显示子行；折叠时同步隐藏所有孙节点。
  - **修复 Top5 WTW 逻辑**：`top5_customers` 中合并当前版与对比版的客户索引 `all_cust = set(cc.index) | set(cm.index)`，确保流失客户（当前 0、对比有值）的负向变动也被纳入排序。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）； skill `ops-excel-merger` 已同步。
- 待用户刷新页面验证实际折叠效果与 Top5 数值是否符合预期。

## 第三十一轮（FCST 分析：修正完成率口径 + 看板美化 + 自动切换导出 Tab）
- 需求（4 点）：
  1. DG%/Quota% 完成率计算口径修正：FY26Q2 DG=445,689、Week9 FCST=473,087，不应算成 94.2%。
  2. 左侧点击【运行筛选/透视】后，右侧自动切换到「明细 / 透视导出」Tab。
  3. FCST 分析顶部三个筛选控件（财年财季、当前 Cycle、对比 Cycle）放在一行展示。
  4. FCST 分析界面美化：像看板；层级颜色区分；WTW/DG%/Quota%/YOY% 红绿色；TTL/APOS/POS 加粗；大区/客户行字号缩小。
- 实现（`app.py`）：
  - **完成率口径**：DG% / Quota% 由 `DG/FCST` 改为 `FCST/DG`（即 amt/dg、amt/q），表示 FCST 对 DG/Quota 目标的完成率。以用户数据验算：473087/445689≈106.1%。
  - **自动切换 Tab**：右侧 `st.tabs` 改为 `st.radio` 横向导航（存入 `active_tab`）；点击【运行筛选/透视】时设置 `run_export=True` 与 `active_tab=明细/透视导出` 并 `st.rerun()`，rerun 后执行导出逻辑并展示结果。
  - **筛选控件一行**：`render_fcst_analysis` 中财年财季、当前 Cycle、对比 Cycle 用 `st.columns([3,1,1])` 横向排列。
  - **看板美化**：
    - 顶部增加 6 个 `st.metric` KPI 卡片（当前FCST/上版FCST/WTW/DG%/Quota%/YOY%）。
    - 树表 CSS 增加 `.up`（绿色）/ `.down`（红色）单元格类；WTW>0 绿、<0 红；DG%/Quota%>100% 绿、<100% 红；YOY%>0 绿且前面加 +、<0 红。
    - TTL/APOS/POS 行 `font-weight: 700`；不同层级左侧加不同颜色竖条区分。
    - 大区（level 3）/ 客户（level 4）行字号缩小到 12px。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步。

## 第三十二轮（FCST 分析：修复导出内容串位 + 百分比整数 + 对齐缩小 + 去 TTL 行 + 看板美化）
- 需求（5 点）：
  1. FCST 分析 Tab 下方不应出现导出界面的内容（筛选结果、明细预览等）。
  2. DG%、Quota%、YOY 百分比不保留小数。
  3. 财年财季 / 当前 Cycle / 对比 Cycle 三行对不齐，且控件需要缩小。
  4. 顶部 KPI 看板已展示 TTL，树表中不再重复显示 TTL 行。
  5. KPI 看板太丑，参考截图美化。
- 实现（`app.py`）：
  - **修复导出逻辑位置**：把原先放在 `if/else` 之后的导出处理代码整体缩进，移入「明细 / 透视导出」Tab 的 `else` 分支内，FCST 分析 Tab 不再渲染导出内容。
  - **百分比整数化**：`_pct` 由 `.1f%` 改为 `.0f%`；`_build_section` 中 YOY  likewise。
  - **筛选区对齐与缩小**：`_segmented_buttons` 的 label 由 `st.caption` 改为 `st.markdown("<small>...</small>")`；两个 Cycle `selectbox` 也改为 `<small>` markdown label + `label_visibility="collapsed"`，保证三者 label 样式和顶部对齐；并注入全局 CSS 缩小按钮 padding/font-size。
  - **树表去掉 TTL 行**：`_build_section` 不再添加 TTL 行，APOS/POS 提升为顶层节点（level 0），产线大类 level 1，大区 level 2，客户 level 3；默认折叠 level>=2。
  - **KPI 看板美化**：新增 `_render_kpi_dashboard`，用 HTML/CSS 渲染 6 张卡片（当前FCST/上版FCST/WTW/DG%/Quota%/YOY%），带圆角、阴影、副标题；WTW/DG%/Quota%/YOY 按红绿规则着色。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步。

## 2026-08-28 15:40 第三十一轮：清理冗余文件 + 修复 FCST 看板 TTL 汇总
- **问题 1/2/3：文件清理**
  - `_streamlit_uploads/`：Streamlit 文件上传器的临时缓存目录，处理完成后可清理（已清空，约 2.5GB）。
  - `.parquet` 文件说明：`data_buckets/*.parquet` 与 `mapping_table.parquet` 是数据池和 Mapping，不可删；`最终合并表.parquet` 是旧版主表产物，已删。
  - 已删除的不影响程序的文件：`_streamlit_uploads/`、`_FY25Q1Union.xlsx`、`_test_force.log`、`_test_force.py`、`ops-excel-merger.zip`、`run.log`、`streamlit.log`、`最终合并表.parquet`、`最终合并表.xlsx`、`逻辑梳理.xlsx`、`__pycache__/`（共释放约 2.9GB）。
- **问题 4：FCST 看板 TTL 汇总**
  - 修复 `_build_section`：在构建树表前单独计算 TTL 汇总（`ttl_summary`），但不把 TTL 行加入树表；树表仍从 APOS/POS 开始。
  - 修改 `compute_fcst`：返回结果中新增 `ttl_summary` 字段。
  - 修改 `_render_kpi_dashboard` 调用：使用 `res["ttl_summary"]` 而非 `main_table.iloc[0]`，确保 KPI 看板展示的是 APOS+POS 的 TTL 合计。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步。

## 2026-08-28 16:03 第三十二轮：FCST by Week 趋势图
- 在 FCST 分析 Tab 树表下方新增"FCST by Week 趋势"图（`_render_fcst_trend`）。
- 数据：复用缓存 `_load_fcst_analysis_data(fy)`，按 scope/sub_region 过滤后仅保留 APOS/POS。
- 趋势线：TTL 合计、APOS、POS，以及 APOS+POS 范围内跨所有 Week×Cycle 客户名称合计 > 500K 的头部客户（最多 15 条，避免拥挤）。
- X 轴为 FCST Cycle（Week），用自然排序（提取数字）保证 Week1..Week15 顺序正确。
- 图例横向置顶，`hovermode="x unified"`。
- 部署咨询：当前为本地 `localhost:8501`，仅本机可访问；分享他人需部署到可公网/内网访问的服务器（Streamlit Community Cloud / 云服务器 + 反代 / 内网穿透 ngrok 等），或 CloudStudio（静态站为主，Streamlit 需容器支持）。

- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步。

## 2026-08-28 16:41 第三十三轮：FCST by Week 改为表格式趋势（含 OneDrive 部署答疑）
- **用户需求**：去掉 Plotly 折线图，改为"像上线表一样"的表格式趋势——行=口径（TTL/APOS/POS + APOS 下大客户），列=各 Week 金额，最右侧= sparkline 趋势；APOS 下钻客户条件改为"任一 Cycle 的 APOS 金额 > 500K"。
- **实现**：重写 `_render_fcst_trend`：
  - 行定义：TTL、APOS、POS，以及 APOS 中"任一 (客户名称, FCST Cycle) 金额 > 500K"的客户（按 APOS 总额排序，Top 20 展示，多于此显示提示）。
  - 列：各 Week 金额（用 `_money` 千分位），最右 "Trend" 列用内联 SVG `polyline` + 圆点渲染 sparkline。
  - 颜色：TTL 黑、APOS 蓝、POS 红、客户灰；客户行缩进 24px + 缩小字号。
  - 表头 sticky，整体滚动；组件高度按行数自适应 `140 + len(rows)*38`。
- **OneDrive 部署答疑**：OneDrive 只是文件同步盘，不能运行 Python 服务；放进去后他人只能"下载文件在自己电脑跑"，无法当网址访问。要给他人当网页用，仍需部署（Streamlit Community Cloud / 云服务器 + Nginx 反代 / ngrok 临时穿透），且需把 `data_buckets/*.parquet` 和 `mapping_table.parquet` 一并带上。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步。

## 2026-08-28 16:48 第三十四轮：FCST by Week 改回参考图风格折线图 + 修复客户未展示
- **问题 1：不要表格式，要参考图风格**
  - 用户参考图（Booking-FCST 趋势）为单条折线+数据标签+DG/Quota 水平参考线+右侧标注。
  - 已把 `_render_fcst_trend` 改回 Plotly 折线图：TTL（黑粗线）、APOS（蓝线）、POS（红线）三条主线均带数据标签；DG/Quota 按当前 scope 对齐后画成水平虚线并右侧标注数值。
  - 样式：白底、浅灰网格、图例横向置顶、hovermode="x unified"，整体更接近参考图。
- **问题 2：没有 by 客户展示**
  - 原因：当前 scope 内满足"任一 Week APOS > 500K"的客户为空，导致客户行完全不出现。
  - 修复：新增 `st.number_input` 阈值控件（默认 500K）；当无客户满足阈值时，自动 fallback 展示 APOS 金额 Top 10 客户，并给出提示"当前范围内无任一 Week APOS > X 的客户，已自动展示 APOS 金额 Top 10"。
  - 大客户线使用 10 色轮播，不显示数据标签避免拥挤，hover 可查看具体数值。
- **问题 3：Streamlit Community Cloud 是否发网址即可**
  - 确认：是的。把代码文件上传到 GitHub 仓库，然后在 Streamlit Community Cloud 连接该仓库，平台会自动部署并生成 `xxx.streamlit.app` 网址，发给别人即可打开使用。
  - 注意事项：必须把数据池文件（`data_buckets/*.parquet`、`mapping_table.parquet`）也放进仓库或部署后手动上传到服务器，否则页面打开是空的。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步。

## 2026-08-28 17:16 第三十五轮：FCST by Week 层级 sparkline 表 + 阈值修正 + 控件缩小
- **问题 1：改为层级 sparkline 表（手绘图样式）**
  - 用户手绘图示意：TTL → APOS + 其客户 → POS + 其客户，每行一条折线，主行标注部分数值。
  - 已第三次重写 `_render_fcst_trend`：用 HTML/SVG 渲染层级表，每行一个内联折线。
  - 层级：TTL（黑）、APOS（蓝）+ 缩进客户、POS（红）+ 缩进客户。
  - 主行（TTL/APOS/POS）在折线上标注首周、末周、最大值；客户行仅画线+圆点。
  - 数据源单位为 USDK，因此"500K"对应数值 500，已将默认阈值由 500000 修正为 500。
- **问题 2：筛选控件太大**
  - 缩小 FCST 分析区域内 `.stButton > button` 的 padding、font-size、min-height、line-height、border-radius，使横向按钮组更紧凑。
- **问题 3：GitHub 仓库后续维护**
  - 确认：把当前 Streamlit 所有东西（`app.py`、`ops_data_processor.py`、`requirements.txt`、`data_buckets/`、`mapping_table.parquet` 等）放进仓库后，可以给我发指令直接改仓库。
  - 前提：我需要对该仓库有写权限（你是 owner 把我加为 collaborator，或给我 token），或者你把仓库地址给我后我通过 PR 方式提交修改。
  - 建议：日常小迭代仍可先在本地（当前 `C:\Users\fenglx1\WorkBuddy\2026-08-25-14-42-27`）调试验证，确认没问题后再同步到 GitHub 部署。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步。

## 2026-08-28 17:34 第三十六轮：趋势表全行标签 + APOS/POS 折叠 + 部署仓库整理
- **问题 1：趋势表每行都加数字标识**
  - 用户参考图显示客户行也需要首/末/最大值标签。
  - 修改 `_render_fcst_trend` 的 `sparkline` 函数：所有行都显示首周、末周、最大值；主行字号 10px，客户行字号 9px。
- **问题 2：APOS/POS 可折叠客户行**
  - 在 APOS/POS 主行 label 前增加 `tree-toggle`（▼/▶）。
  - 客户行加 `child-apos` / `child-pos` class 与 `data-parent` 属性。
  - 内嵌 JS：点击 toggle 切换子行 `display`，默认展开。
- **问题 3：整理 Streamlit Community Cloud 部署仓库**
  - 创建 `requirements.txt`、`.streamlit/config.toml`、`README.md`。
  - 修改 `app.py` 中 `ROOT` 为相对路径 `os.path.dirname(os.path.abspath(__file__))`。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步。

## 2026-08-28 17:54 第三十七轮：FCST by Week 表格式 + 本地 git 仓库初始化
- **问题 1：趋势图改为表格式**
  - 用户希望顶部显示 Week/CYCLE 列标题，且每个 Week 的金额都显示。
  - 重写 `_render_fcst_trend`：改为 HTML 表格，列为 口径 | Week1 | Week2 | ... | WeekN | Trend。
  - 每行每个 Week 显示金额（千分位），最右侧为内联 sparkline。
  - 整体字体缩小（表头 11px、金额 10px、口径 11-12px），并加 `overflow-x: auto` 横向滚动。
  - 保留 APOS/POS 折叠交互（▼/▶）。
- **问题 2：GitHub 仓库在哪**
  - 当前仅在本地；已在项目目录初始化 git 仓库并提交首次 commit。
  - 因环境无 GitHub CLI 且未配置 token，无法直接创建 GitHub 远程仓库；已给出推送到 GitHub 的完整命令（用户需在 GitHub 网页新建空仓库后执行）。
- 验证：编译通过；Streamlit 干净重启（HTTP 200）；skill 已同步；本地 git 仓库已初始化。
