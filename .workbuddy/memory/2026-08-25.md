# 2026-08-25 工作日志

## OPS 数据统一合并 Streamlit 工具

- 读取并学习了 `C:\Users\fenglx1\OneDrive - Lenovo\1 - 自用\0-OPS\逻辑梳理.xlsx` 中的表头映射与取数逻辑。
- 扫描了数据目录 `C:\Users\fenglx1\OneDrive - Lenovo\1 - 自用\0-OPS\数据统一格式`，确认源文件：
  - 历史Union.xlsx、QTD.xlsx、T1 FCST SDA&STB.xlsx、T2 FCST.xlsx、T3 FCST.xlsx、T1 HB&JV.xlsx、DG&Quota.xlsx。
- 实现核心处理模块 `ops_data_processor.py`，按类别完成字段映射、服务大区清洗、POS/APOS 判断、产线+通路推导等逻辑。
- 成功合并 550,548 行数据（ACT 539,765 行 + FCST 10,783 行），DG&Quota.xlsx 当前为空未产生数据。
- 开发 Streamlit 应用 `app.py`，支持本地文件夹扫描、上传文件、合并导出、多维度图表分析。
- 启动 Streamlit 服务：http://localhost:8501。
- 将本次多表合并流程保存为用户级 skill `ops-excel-merger`，打包为 `ops-excel-merger.zip`。

## 第二轮：按用户 13 条修正更新取数逻辑

用户对首版合并表提出 13 条修正，已全部实现并验证：
1. ACT 剔除纯"财务结算"行（仅保留含"业绩考核"）。
2. 全局剔除服务大区为空白的行。
3/7. FCST 剔除"是否FCST"为 否/商机 的行。
4. 物料通路、产线+通路 中 Softbundle→STB、SDA-新阳光→SDA（全局替换）。
5. 修复 QTD 服务大区被误清空：移除 clean_region 关键词过滤（"其他/产线/行业集成"本是有效值）。
6. 产线类型=HW 时，产线+通路 取 KAB产品组 值。
8. T1 FCST POS_APOS 按销售通路推导：SDA→APOS、STB→STB（原列本为空）。
9. T2 服务大区空白根因同 5（关键词过滤），已修复；产线类型恒为 T2。
10. T2 Sales：MML-租赁=是→SSG POS OS，否则→SSG APOS OS。
11. T2 产线+通路：租赁且含 运维服务_DWS→Windows；租赁且含 TruScale/PC DaaS→External Funder；否则取 新分类-区域。
12. T3 IDGISG：中性→ISG，否则取原值。
13. T3 仅保留 服务收入KUSD去税≠0 或 硬件收入KUSD去税≠0 的明细；服务≠0→APOS、硬件≠0→POS，两列均非0的同一源行展开为两行（每行同时带 POS_APOS 与金额，金额入 业绩考核USDK）。

- 重新运行后最终合并表 547,666 行（ACT 539,516 / FCST 7,610 / DG 540）。DG&Quota 本次有 540 行（DG）。
- 已同步更新 skill 脚本，并重启 Streamlit（端口 8501）。
- 注意：首轮 Streamlit 实际跑在 8502，已清理并在 8501 重启干净实例。
